schema: http://localhost:9120/api/graphql
documents: ["src/**/*.ts", "!src/gql/**/*"]
generates:
  ./src/gql/index.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      avoidOptionals: true
